# Getting Started with Achilles Node App

## Local Commands
`yarn install`
`yarn build`
`yarn start`

## Docker commands
`docker-compose up -d --build`

After running commands successfully, the app will be run on http://localhost:4000